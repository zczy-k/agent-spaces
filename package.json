{
  "name": "agent-spaces",
  "version": "0.2.6",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"pnpm --filter @agent-spaces/server dev\" \"pnpm --filter @agent-spaces/web dev\"",
    "build": "pnpm --filter @agent-spaces/shared build && pnpm --filter @agent-spaces/sdk build && pnpm --filter @agent-spaces/server build && cross-env NEXT_STATIC_EXPORT=1 pnpm --filter @agent-spaces/web build && node scripts/copy-web.mjs && node scripts/copy-package.mjs",
    "build:docker": "docker build -f Dockerfile.server -t agent-spaces-server:latest .",
    "up": "docker compose up -d --build",
    "lint": "pnpm -r lint",
    "publish": "pnpm --filter @agent-spaces/shared build && pnpm --filter @agent-spaces/server build && pnpm --filter @agent-spaces/shared publish && pnpm --filter @agent-spaces/server publish",
    "clean": "rm -rf packages/*/dist packages/*/.next packages/server/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^10.1.0",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "msw",
      "node-pty",
      "protobufjs",
      "sharp",
      "unrs-resolver"
    ]
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
